require "spec_helper"

describe Activerecord::Preload::Economizer do
  it "has a version number" do
    expect(Activerecord::Preload::Economizer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
